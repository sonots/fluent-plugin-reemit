<source>
  type gc_stat
  emit_interval 1s
  tag gc_stat
</source>

<match **>
  type copy
  <store>
    type stdout
  </store>
  <store>
    type reemit # re-emitted messages are not absorbed by <match **>
  </store>
</match>

<match **>
  type file
  path gc_stat.txt
</match>
