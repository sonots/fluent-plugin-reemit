<source>
  type gc_stat
  emit_interval 1s
  tag gc_stat
</source>

<match flowcount>
  type stdout
</match>

<match **>
  type copy
  <store>
    type flowcounter
    count_keys *
  </store>
  <store>
    type reemit # re-emitted messages are not absorbed by <match **>
  </store>
</match>

<match **>
  type stdout
</match>
